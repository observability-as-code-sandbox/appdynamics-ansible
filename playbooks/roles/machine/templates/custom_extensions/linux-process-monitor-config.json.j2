{
    "ConfigItems": 
        [
   
            {
                "Name": "appNameOrID", 
                "Value": "{{ process_monitor_appname }}"   
            }, 

            {
                "Name": "controllerHost", 
                "Value": "{{ controller_host_name }}:{{controller_port}}"   
            },

            {
                "Name": "OAuthToken", 
                "Value": "{{ process_monitor_oauthtoken }}"   
            }
        ]
    }